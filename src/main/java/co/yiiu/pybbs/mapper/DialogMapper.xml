<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yiiu.pybbs.mapper.DialogMapper">
    <select id="selectByUserId" resultType="map">
        select d.*, u.username, u.avatar
        from dialog d
                 left join user u on d.user_a_id = u.id
        where d.user_a_id = #{userId} or d.user_b_id = #{userId}
        order by d.in_time desc
    </select>
</mapper>